export default function RDLabsWorkspace() {
  console.log("RDLabsWorkspace is rendering");

  return (
    <div className="w-full h-full flex items-center justify-center bg-orange-500" style={{ minHeight: "500px" }}>
      <div className="text-center">
        <p className="text-4xl font-bold text-black">ORANGE TEST - RDLABS WORKSPACE</p>
        <p className="text-xl text-black mt-4">RDLabsWorkspace is now rendering!</p>
      </div>
    </div>
  );
}
